import {
  bigint,
  bigserial,
  bit,
  boolean,
  bytea,
  char,
  cidr,
  date,
  decimal,
  doublePrecision,
  geometry,
  halfvec,
  inet,
  integer,
  interval,
  json,
  jsonb,
  line,
  macaddr,
  macaddr8,
  numeric,
  pgEnum,
  pgSchema,
  pgTable,
  point,
  real,
  serial,
  smallint,
  smallserial,
  sparsevec,
  text,
  time,
  timestamp,
  uuid,
  varchar,
  vector,
} from 'drizzle-orm/pg-core';
import { createSelectSchema } from 'drizzle-orm/zod';

export const moodEnum = pgEnum('fixture_pg_data_types_mood', ['sad', 'ok', 'happy']);

export const pgDataTypes = pgTable('pg_data_types', {
  id: serial('id').primaryKey(),
  smallIntCol: smallint('small_int_col'),
  integerCol: integer('integer_col'),
  serialCol: serial('serial_col'),
  smallSerialCol: smallserial('small_serial_col'),
  bigIntNumberCol: bigint('big_int_number_col', { mode: 'number' }),
  bigIntBigIntCol: bigint('big_int_bigint_col', { mode: 'bigint' }),
  bigIntStringCol: bigint('big_int_string_col', { mode: 'string' }),
  bigSerialNumberCol: bigserial('big_serial_number_col', { mode: 'number' }),
  bigSerialBigIntCol: bigserial('big_serial_bigint_col', { mode: 'bigint' }),
  realCol: real('real_col'),
  doublePrecisionCol: doublePrecision('double_precision_col'),
  numericStringCol: numeric('numeric_string_col'),
  numericNumberCol: numeric('numeric_number_col', { mode: 'number' }),
  numericBigIntCol: numeric('numeric_bigint_col', { mode: 'bigint' }),
  decimalStringCol: decimal('decimal_string_col'),
  decimalNumberCol: decimal('decimal_number_col', { mode: 'number' }),
  decimalBigIntCol: decimal('decimal_bigint_col', { mode: 'bigint' }),
  booleanCol: boolean('boolean_col'),
  textCol: text('text_col'),
  textEnumCol: text('text_enum_col', { enum: ['alpha', 'beta', 'gamma'] }),
  varcharCol: varchar('varchar_col', { length: 64 }),
  varcharEnumCol: varchar('varchar_enum_col', { enum: ['red', 'green', 'blue'], length: 16 }),
  charCol: char('char_col', { length: 3 }),
  charEnumCol: char('char_enum_col', { enum: ['AA', 'AB', 'AC'], length: 2 }),
  uuidCol: uuid('uuid_col'),
  byteaCol: bytea('bytea_col'),
  jsonCol: json('json_col'),
  jsonbCol: jsonb('jsonb_col'),
  dateCol: date('date_col', { mode: 'date' }),
  dateStringCol: date('date_string_col', { mode: 'string' }),
  timeCol: time('time_col'),
  timeTzCol: time('time_tz_col', { withTimezone: true }),
  timestampCol: timestamp('timestamp_col'),
  timestampStringCol: timestamp('timestamp_string_col', { mode: 'string' }),
  timestampTzCol: timestamp('timestamp_tz_col', { withTimezone: true }),
  timestampTzStringCol: timestamp('timestamp_tz_string_col', {
    withTimezone: true,
    mode: 'string',
  }),
  intervalCol: interval('interval_col'),
  cidrCol: cidr('cidr_col'),
  inetCol: inet('inet_col'),
  macaddrCol: macaddr('macaddr_col'),
  macaddr8Col: macaddr8('macaddr8_col'),
  pointTupleCol: point('point_tuple_col'),
  pointObjectCol: point('point_object_col', { mode: 'xy' }),
  lineTupleCol: line('line_tuple_col'),
  lineAbcCol: line('line_abc_col', { mode: 'abc' }),
  geometryTupleCol: geometry('geometry_tuple_col', { type: 'point' }),
  geometryObjectCol: geometry('geometry_object_col', { type: 'point', mode: 'xy' }),
  vectorCol: vector('vector_col', { dimensions: 3 }),
  halfvecCol: halfvec('halfvec_col', { dimensions: 3 }),
  sparsevecCol: sparsevec('sparsevec_col', { dimensions: 3 }),
  bitCol: bit('bit_col', { dimensions: 8 }),
  moodCol: moodEnum('mood_col'),
});

export const realtimeSchema = pgSchema('realtime');

export const eventsPublicPgDataTypes = realtimeSchema.table('events_public_pg_data_types', {
  id: integer('id').notNull(),
  smallIntCol: smallint('small_int_col'),
  integerCol: integer('integer_col'),
  serialCol: integer('serial_col'),
  smallSerialCol: integer('small_serial_col'),
  bigIntNumberCol: bigint('big_int_number_col', { mode: 'number' }),
  bigIntBigIntCol: bigint('big_int_bigint_col', { mode: 'bigint' }),
  bigIntStringCol: bigint('big_int_string_col', { mode: 'string' }),
  bigSerialNumberCol: bigint('big_serial_number_col', { mode: 'number' }),
  bigSerialBigIntCol: bigint('big_serial_bigint_col', { mode: 'bigint' }),
  realCol: real('real_col'),
  doublePrecisionCol: doublePrecision('double_precision_col'),
  numericStringCol: numeric('numeric_string_col'),
  numericNumberCol: numeric('numeric_number_col', { mode: 'number' }),
  numericBigIntCol: numeric('numeric_bigint_col', { mode: 'bigint' }),
  decimalStringCol: decimal('decimal_string_col'),
  decimalNumberCol: decimal('decimal_number_col', { mode: 'number' }),
  decimalBigIntCol: decimal('decimal_bigint_col', { mode: 'bigint' }),
  booleanCol: boolean('boolean_col'),
  textCol: text('text_col'),
  textEnumCol: text('text_enum_col', { enum: ['alpha', 'beta', 'gamma'] }),
  varcharCol: varchar('varchar_col', { length: 64 }),
  varcharEnumCol: varchar('varchar_enum_col', { enum: ['red', 'green', 'blue'], length: 16 }),
  charCol: char('char_col', { length: 3 }),
  charEnumCol: char('char_enum_col', { enum: ['AA', 'AB', 'AC'], length: 2 }),
  uuidCol: uuid('uuid_col'),
  byteaCol: bytea('bytea_col'),
  jsonCol: json('json_col'),
  jsonbCol: jsonb('jsonb_col'),
  dateCol: date('date_col', { mode: 'date' }),
  dateStringCol: date('date_string_col', { mode: 'string' }),
  timeCol: time('time_col'),
  timeTzCol: time('time_tz_col', { withTimezone: true }),
  timestampCol: timestamp('timestamp_col'),
  timestampStringCol: timestamp('timestamp_string_col', { mode: 'string' }),
  timestampTzCol: timestamp('timestamp_tz_col', { withTimezone: true }),
  timestampTzStringCol: timestamp('timestamp_tz_string_col', {
    withTimezone: true,
    mode: 'string',
  }),
  intervalCol: interval('interval_col'),
  cidrCol: cidr('cidr_col'),
  inetCol: inet('inet_col'),
  macaddrCol: macaddr('macaddr_col'),
  macaddr8Col: macaddr8('macaddr8_col'),
  pointTupleCol: point('point_tuple_col'),
  pointObjectCol: point('point_object_col', { mode: 'xy' }),
  lineTupleCol: line('line_tuple_col'),
  lineAbcCol: line('line_abc_col', { mode: 'abc' }),
  geometryTupleCol: geometry('geometry_tuple_col', { type: 'point' }),
  geometryObjectCol: geometry('geometry_object_col', { type: 'point', mode: 'xy' }),
  vectorCol: vector('vector_col', { dimensions: 3 }),
  halfvecCol: halfvec('halfvec_col', { dimensions: 3 }),
  sparsevecCol: sparsevec('sparsevec_col', { dimensions: 3 }),
  bitCol: bit('bit_col', { dimensions: 8 }),
  moodCol: moodEnum('mood_col'),
  $old_id: integer('$old_id'),
  $old_small_int_col: smallint('$old_small_int_col'),
  $old_integer_col: integer('$old_integer_col'),
  $old_serial_col: integer('$old_serial_col'),
  $old_small_serial_col: integer('$old_small_serial_col'),
  $old_big_int_number_col: bigint('$old_big_int_number_col', { mode: 'number' }),
  $old_big_int_bigint_col: bigint('$old_big_int_bigint_col', { mode: 'bigint' }),
  $old_big_int_string_col: bigint('$old_big_int_string_col', { mode: 'string' }),
  $old_big_serial_number_col: bigint('$old_big_serial_number_col', { mode: 'number' }),
  $old_big_serial_bigint_col: bigint('$old_big_serial_bigint_col', { mode: 'bigint' }),
  $old_real_col: real('$old_real_col'),
  $old_double_precision_col: doublePrecision('$old_double_precision_col'),
  $old_numeric_string_col: numeric('$old_numeric_string_col'),
  $old_numeric_number_col: numeric('$old_numeric_number_col', { mode: 'number' }),
  $old_numeric_bigint_col: numeric('$old_numeric_bigint_col', { mode: 'bigint' }),
  $old_decimal_string_col: decimal('$old_decimal_string_col'),
  $old_decimal_number_col: decimal('$old_decimal_number_col', { mode: 'number' }),
  $old_decimal_bigint_col: decimal('$old_decimal_bigint_col', { mode: 'bigint' }),
  $old_boolean_col: boolean('$old_boolean_col'),
  $old_text_col: text('$old_text_col'),
  $old_text_enum_col: text('$old_text_enum_col', { enum: ['alpha', 'beta', 'gamma'] }),
  $old_varchar_col: varchar('$old_varchar_col', { length: 64 }),
  $old_varchar_enum_col: varchar('$old_varchar_enum_col', {
    enum: ['red', 'green', 'blue'],
    length: 16,
  }),
  $old_char_col: char('$old_char_col', { length: 3 }),
  $old_char_enum_col: char('$old_char_enum_col', { enum: ['AA', 'AB', 'AC'], length: 2 }),
  $old_uuid_col: uuid('$old_uuid_col'),
  $old_bytea_col: bytea('$old_bytea_col'),
  $old_json_col: json('$old_json_col'),
  $old_jsonb_col: jsonb('$old_jsonb_col'),
  $old_date_col: date('$old_date_col', { mode: 'date' }),
  $old_date_string_col: date('$old_date_string_col', { mode: 'string' }),
  $old_time_col: time('$old_time_col'),
  $old_time_tz_col: time('$old_time_tz_col', { withTimezone: true }),
  $old_timestamp_col: timestamp('$old_timestamp_col'),
  $old_timestamp_string_col: timestamp('$old_timestamp_string_col', { mode: 'string' }),
  $old_timestamp_tz_col: timestamp('$old_timestamp_tz_col', { withTimezone: true }),
  $old_timestamp_tz_string_col: timestamp('$old_timestamp_tz_string_col', {
    withTimezone: true,
    mode: 'string',
  }),
  $old_interval_col: interval('$old_interval_col'),
  $old_cidr_col: cidr('$old_cidr_col'),
  $old_inet_col: inet('$old_inet_col'),
  $old_macaddr_col: macaddr('$old_macaddr_col'),
  $old_macaddr8_col: macaddr8('$old_macaddr8_col'),
  $old_point_tuple_col: point('$old_point_tuple_col'),
  $old_point_object_col: point('$old_point_object_col', { mode: 'xy' }),
  $old_line_tuple_col: line('$old_line_tuple_col'),
  $old_line_abc_col: line('$old_line_abc_col', { mode: 'abc' }),
  $old_geometry_tuple_col: geometry('$old_geometry_tuple_col', { type: 'point' }),
  $old_geometry_object_col: geometry('$old_geometry_object_col', { type: 'point', mode: 'xy' }),
  $old_vector_col: vector('$old_vector_col', { dimensions: 3 }),
  $old_halfvec_col: halfvec('$old_halfvec_col', { dimensions: 3 }),
  $old_sparsevec_col: sparsevec('$old_sparsevec_col', { dimensions: 3 }),
  $old_bit_col: bit('$old_bit_col', { dimensions: 8 }),
  $old_mood_col: moodEnum('$old_mood_col'),
  $snapshot: integer('$snapshot').generatedAlwaysAsIdentity(),
  $op: text('$op').notNull(),
  $timestamp: timestamp('$timestamp', { withTimezone: true }).notNull().defaultNow(),
});

export const pgDataTypesRowSchema = createSelectSchema(pgDataTypes);
